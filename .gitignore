# Credentials — never commit
service_account.json
client_secret.json
drive_token.json
.env
grokapi.env
*.key

# Claude Code local settings
.claude/

# Python cache
__pycache__/
*.pyc

# Generated outputs (large binaries — regenerate with the scripts)
*.mp3
*.mp4
*.jpg
*.jpeg
*.png

# Image output folders
script1_images/

# Grok X query text outputs
x_query_outputs/

# Grok notebook sketch outputs (keep prompt templates in git)
notebook_sketches/*.jpg
notebook_sketches/*.jpeg
notebook_sketches/*.png

# Script/data folders (content files, not code)
script naval chola - audio , images and video/
voice and image prompts/
